#OUTPUT "PETWOLF"'--------.DECLARES'--------#DECLARE A$, GAME¯OVER&, PLYR¯IDX#DECLARE PX(3), PY(3), PF(3), PA(3)#DECLARE PADDLE¯X, PADDLE¯Y, PADDLE¯F'-------.DEFINES'-------'----.MAIN'----  GOSUB TITLE  GOSUB GAME  GOTO MAIN'-----.TITLE'-----  PRINT CHR$(27);"5“";  PRINT "PET WOLF - BY GURCE ISIKYILDIZ"  PRINT "========"  PRINT  PRINT "A PADDLE GAME FOR UP TO 4 PLAYERS, BASED ON MIDWAY/BALLY'S CLASSIC!"  PRINT  PRINT "PRESS A KEY"  GET KEY A$  RETURN'----.INIT'----  GAME¯OVER& = 0  FOR PLYR¯IDX = 0 TO 3    PF(PLYR¯IDX) = 0    PA(PLYR¯IDX) = 0  NEXT PLYR¯IDX  RETURN'----.GAME'----  GOSUB INIT  DO    GOSUB USER¯INPUT    GOSUB DRAW¯CONTENT    GOSUB CHECK¯COLLISION  LOOP WHILE GAME¯OVER& = 0  RETURN'----------.USER¯INPUT'----------  FOR PLYR¯IDX = 0 TO 3    GOSUB READ¯PADDLE    PX(PLYR¯IDX) = PADDLE¯X    PY(PLYR¯IDX) = PADDLE¯Y    IF PADDLE¯F = 1 AND PF(PLYR¯IDX) = 0 THEN BEGIN      GOSUB FIRE¯MISSILE    BEND    PF(PLYR¯IDX) = PADDLE¯F  NEXT PLYR¯IDX  RETURN'-----------.READ¯PADDLE'-----------  RETURN'------------.FIRE¯MISSILE'------------  RETURN'------------.DRAW¯CONTENT'------------  FOR PLYR¯IDX = 0 TO 3      NEXT PLYR¯IDX  RETURN'---------------.CHECK¯COLLISION'---------------  RETURN'---------.GAME¯OVER'---------  PRINT "GAME OVER"  GET KEY A$  RETURN